using System;

namespace cshape_in_one_day
{
    class Program
    {
        static void Main(string[] args)
        {
            //Array, String, list
            // int [] userAge = {20, 30, 40, 50};
            // for(int i = 0; i < userAge.Length; i++){
            //     Console.WriteLine(userAge[i]);
            // }

            //Properties of array

            // int [] subjectMark = {80, 75, 90, 87, 77, 79};
            // int length = subjectMark.Length;
            // Console.WriteLine("Array Length: "+ length);

            int [] source = {3, 1, 4, 5, 7, 6};
            int [] destiny = {20, 30, 40, 70, 55};

            // Array.Copy(source, destiny, 2);

            // foreach (var item in destiny)
            // {
            //     Console.WriteLine(item);
            // }

            // Array.Sort(source);
            // foreach (var item in source)
            // {
            //     Console.WriteLine(item);
            // }

            // int ans = Array.IndexOf(destiny, 90);
            // if(ans == -1){
            //     Console.WriteLine("Number is not found");
            // }
            // else{
            //     Console.WriteLine("Number is found in : " + ans +" no index");
            // }


            
            // string [] name = new string [5];
            // Console.WriteLine(name.Length);
            // Console.WriteLine("Enter your 5 friends name");
            // for(int i = 0; i < name.Length; i++){
            //     name[i] = Console.ReadLine();
            // }
            
            // string [] name = {"Emdadul", "Hoque", "Tareque"};
            // Console.WriteLine(name[0]);
            // Console.WriteLine(name[1]);
            // Console.WriteLine(name[2]);

            // Console.Write(name[0]);
            // Console.Write(name[1]);
            // Console.Write(name[2]);

            
        }
    }
}
